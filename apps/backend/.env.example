# Server Configuration
PORT=3001

# Environment
# Options: development, production
NODE_ENV=development

# Frontend URL(s) for CORS
# For multiple origins, use comma-separated values: http://localhost:5177,http://localhost:5173
# Defaults to http://localhost:5177,http://localhost:5173 if not set
FRONTEND_URL=http://localhost:5177,http://localhost:5173

# Database Configuration
# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database
# Defaults to postgresql://postgres:postgres@localhost:5432/ai_chat_rag if not set
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/ai_chat_rag

# Clerk Authentication
# Get your secret key from https://dashboard.clerk.com
# Required for authentication endpoints
CLERK_SECRET_KEY=sk_test_your_clerk_secret_key_here

# Admin User ID (Fallback)
# Used as fallback user ID when requests are not authenticated
# This allows the API to work for unauthenticated users in development
ADMIN_USER_ID=admin_user_id_here
